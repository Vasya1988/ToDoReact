import classes from '../forwardButton/ForwardButton.module.css';

const ForwardButton = (props) => {
    return (
        <button
            className={classes.ForwardButton}
        >
        </button>
    )
}
export default ForwardButton;