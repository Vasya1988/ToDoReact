import classes from '../backButton/BackButton.module.css';

const BackButton = (props) => {
    return (
        <button
            className={classes.BackButton}
        >
        </button>
    )
}
export default BackButton;