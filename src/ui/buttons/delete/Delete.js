import classes from '../delete/Delete.module.css'

const Delete =(props) => {
    return (
        <button
            className={classes.Delete}
        >
            Delete
        </button>
    )
}

export default Delete;